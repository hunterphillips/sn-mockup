# ServiceNow Instance Configuration for Table Import
# Copy this file to .env and fill in your ServiceNow credentials

# Your ServiceNow instance URL (e.g., https://dev12345.service-now.com)
VITE_SN_INSTANCE=https://your-instance.service-now.com

# ServiceNow username (needs read access to tables)
VITE_SN_USERNAME=admin

# ServiceNow password
VITE_SN_PASSWORD=your-password

# Now Assist AI Configuration
# Provider: anthropic | openai | google | ollama
VITE_AI_PROVIDER=anthropic

# Optional: Override default model for the provider
VITE_AI_MODEL=

# API Keys (server-side only - set the one for your chosen provider)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
# Note: Ollama runs locally and doesn't need an API key

# AI Tools
# Serper API key for web_search tool (https://serper.dev)
SERPER_API_KEY=